instrument:
  address: "MOCK::INSTRUMENT"
  link_loss_db: 10.0

sweep:
  launch_powers_dbm: [-5, -3, -1, 0, 1, 3, 5]

thresholds:
  osnr_db: 15.0
  ber: 0.15

paths:
  reports: "reports"
  logs: "logs"